# Production build — loaded by Vite when running `npm run build`
# Anon key is public by design (embedded in the client bundle).

VITE_SUPABASE_URL=https://qnjzliugbtneonutohro.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_ACyCXv-4eQ_PT6tF7vvnQw_YcqectEf

# Public API origin (Nginx → Express on port 4000). Update if your domain differs.
VITE_API_BASE_URL=https://api.level.ph
