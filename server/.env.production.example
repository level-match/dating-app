# Copy to .env.production for the Droplet (npm start).
# Shared secrets (Supabase, DB, admin keys) go in server/.env

NODE_ENV=production
FRONTEND_ORIGIN=https://level.ph
ADMIN_ORIGIN=https://level.ph
ALLOW_DEV_PAYMENT_CONFIRM=false
